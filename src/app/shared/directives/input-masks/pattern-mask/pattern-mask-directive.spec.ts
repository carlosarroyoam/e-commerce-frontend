import { PatternMask } from './pattern-mask-directive';

describe('PatternMask', () => {
  it('should create an instance', () => {
    const directive = new PatternMask();
    expect(directive).toBeTruthy();
  });
});
