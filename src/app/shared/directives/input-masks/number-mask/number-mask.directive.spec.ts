import { NumberMask } from './number-mask.directive';

describe('CurrencyMask', () => {
  it('should create an instance', () => {
    const directive = new NumberMask();
    expect(directive).toBeTruthy();
  });
});
